extends Node

signal arrived_at_meta_path(meta_path_index: int)
signal left_meta_path(meta_path_index: int)
signal camera_moved()

signal skipped_intro()

signal safe_zone_crossed(show_all_buttons: bool)